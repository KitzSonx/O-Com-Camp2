# 🧠 สรุปเนื้อหาและโค้ด สอวน. คอมพิวเตอร์ ค่าย 2 (OI Camp 2)

คลังสรุปเนื้อหา, โค้ดตัวอย่าง, และเทคนิคการแก้ปัญหา จากการเข้าค่าย สอวน. คอมพิวเตอร์ ค่าย 2

เป้าหมายของค่ายนี้ คือ จะบอกว่าเราไม่ได้แข่งกันแค่ว่า "ใครเขียนโค้ดได้" แต่แข่งกันว่า **"ใครเขียนโค้ดได้มีประสิทธิภาพที่สุด"** เนื้อหาจึงเน้นไปที่การวิเคราะห์ **Time Complexity**, การเลือกใช้ **Data Structures** ให้เหมาะกับปัญหา และการออกแบบ **Algorithm** ที่ซับซ้อน เพื่อจัดการข้อมูลขนาดใหญ่ให้ได้ภายในเวลาที่กำหนด

* **แหล่งข้อมูลสไลด์:** สไลด์ประกอบการบรรยายทั้งหมดจะอยู่ที่โฟลเดอร์ `/02 Camp 2 Slides` (หรือ `/slide/Camp 2`)

---

## 📚 Topics Overview

เนื้อหาในค่าย 2 จะแบ่งเป็นกลุ่มหลักๆ ดังนี้:

### 1. Tools & Fundamentals

* **Time Complexity:** หัวใจของค่าย 2 คือการวิเคราะห์อัลกอริทึม (Big O) ว่ามีประสิทธิภาพ (Time/Space) แค่ไหน
* **C++ Standard Template Library (STL):** การใช้ "อาวุธ" ที่มีให้ เช่น `vector`, `map`, `set`, `queue`, `priority_queue`, `stack` รวมถึง `Hash Table` เพื่อแก้ปัญหาได้เร็ว
* **Pointer & Linked List:** เข้าใจพื้นฐานการจัดการหน่วยความจำ (Memory Management) และโครงสร้างข้อมูลแบบเชื่อมโยง

### 2. Data Structures

* **Tree (Basic):** โครงสร้างข้อมูลแบบต้นไม้
* **Binary Tree & Binary Search Tree (BST):** เป็นการค้นหาข้อมูลแบบ Binary (O(log N))
* **Disjoint Set Union (DSU):** เป็นโครงสร้างข้อมูลสำหรับ "การรวมกลุ่ม" และเอาไว้ตรวจสอบว่าอยู่กลุ่มเดียวกันไหม
* **Heap (Priority Queue):** เป็นโครงสร้างข้อมูลที่ใช้หา "ค่าที่น้อยที่สุด/มากที่สุด" (Implement `priority_queue`)

### 3. Algorithms

* **Sorting and Searching:** เทคนิคการเรียงลำดับและค้นหาข้อมูล (เช่น Binary Search)
* **Complete Search (Brute Force):** การค้นหาคำตอบที่เป็นไปได้ทั้งหมด (มักใช้เป็นท่าพื้นฐานก่อนทำ Optimization)
* **Graph Theory:**
    * **Graph Traversal:** การท่องไปในกราฟรูปแบบต่างๆ (BFS, DFS)
    * **Shortest Path:** การหาเส้นทางที่สั้นที่สุด (Dijkstra's, Bellman-Ford)
    * **Minimum Spanning Trees (MST):** การเชื่อมโหนดทั้งหมดด้วยต้นทุนที่น้อยที่สุด (Prim's, Kruskal's)
* **Dynamic Programming (DP):** "การจำเพื่อแก้ปัญหา" เป็นเทคนิคการแก้ปัญหาย่อยแล้วจำคำตอบไว้ เพื่อแก้ปัญหาที่ใหญ่ขึ้น (เป็นหัวข้อที่ยากและสำคัญมาก)
* **Greedy Algorithm:** "การแก้ปัญหาแบบละโมภ" การเลือกทางเลือกที่ดีที่สุดในขณะนั้น และหวังว่ามันจะนำไปสู่คำตอบที่ดีที่สุดในภาพรวม ซึ่งบางครั้งอาจไม่ดี

---

## 💡 How to Prepare

* **อย่าจำแค่โค้ด, ให้เข้าใจ Time Complexity:** ค่าย 2 ไม่ถามว่า "ทำได้ไหม" แต่ถามว่า "ทำได้เร็แค่ไหน" การที่รู้ว่าอัลกอริทึมนี้ทำงานกี่ขั้นตอน (Big O) สำคัญกว่าการจำโค้ดได้
* **STL มีประโยชน์มาก:** ฝึกใช้ `vector`, `map`, `set`, `priority_queue` ให้คล่องจะช่วยประหยัดเวลา Implement ไปได้หลายเท่า
* **Graph และ DP ควรฝึกเยอะๆ:** สองหัวข้อนี้ใครเข้าใจจะได้เปรียบมหาศาล ควรฝึกโจทย์ 2 หัวข้อนี้ให้เยอะที่สุด
